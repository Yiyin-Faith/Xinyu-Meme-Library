import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'com.puff.meme', appName: '心语表情库', webDir: 'dist',
  android: { backgroundColor: '#f1f5f2', allowMixedContent: false },
};
export default config;
